name: Fix Anita mobile title image

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-anita-mobile-title.yml'
      - 'scripts/fix_anita_mobile_title.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fix Anita mobile title layout
        run: python scripts/fix_anita_mobile_title.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Fix Anita mobile title image overlap [skip ci]"
          git push
